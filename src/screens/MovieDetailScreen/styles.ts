import { StyleSheet } from "react-native";

export const cs = StyleSheet.create({
    buttonStyle: {
        marginTop: 20,
        padding: 10,
        borderRadius: 8,
        borderWidth: 1,
        backgroundColor: 'red',
        borderColor: 'red',
        marginHorizontal: 10
    },
})